server:
  port: ${PORT:8080}

spring:
  application:
    name: GarbageCollection

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate   # 🔥 SAFE for production
    show-sql: false        # 🔥 disable in production
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

# 🔐 JWT Configuration
jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000   # 1 hour (in ms)

# 📊 Logging Configuration
logging:
  level:
    root: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql: WARN
#FireBase Configuration
firebase:
  database:
    url: ${FIREBASE_DB_URL}
  credentials: ${FIREBASE_CREDENTIALS}
































































































































































































































##server:
##  context-path: /gb-collection
##spring:
##  application:
##    name: GarbageCollection
##
##  datasource:
##    url: jdbc:postgresql://localhost:5432/smart_garbage
##    username: postgres
##    password: 2006
##    driver-class-name: org.postgresql.Driver
##  jpa:
##    hibernate:
##      ddl-auto: update  # Use 'validate' or 'none' in production
##    show-sql: true  # Optional, to log SQL statements
##    open-in-view: false
##
##
#server:
#  port: ${PORT:8080}
#
#spring:
#  application:
#    name: GarbageCollection
#
#  datasource:
#    url: ${DB_URL}
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update   # later → change to validate
#    show-sql: true
#    open-in-view: false
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#
#logging:
#  level:
#    root: INFO